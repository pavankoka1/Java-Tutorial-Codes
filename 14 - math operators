class apples{
    public static void main(String args[]){
	
	double p = 100, r = 0.01;
        for(int i=1;i<=10;i++){
		System.out.println(p*Math.pow(1  +r, i));
	}
    }
}

//abs, ceil, floor, max, min, pow, sqrt     ||    all can apply after dot seporator
